# scripts/

One-off tooling that doesn't belong in the main backend service.

Planned:
- `generate-qr-tags.py` — bulk QR code generation for the bootstrap inventory audit (phase 0)
- `backup.sh` — the nightly `pg_dump` cron script referenced in [../docs/DEPLOYMENT.md](../docs/DEPLOYMENT.md)
